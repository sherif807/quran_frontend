export const metadata = {
  title: "Semantic Search",
  description:
    "Run semantic scripture search on QuranAlive in English, Arabic, Hebrew, and Greek and jump directly to matching verses.",
  alternates: {
    canonical: "/search",
  },
};

export default function SearchLayout({ children }) {
  return children;
}
